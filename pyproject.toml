[project]
name = "shelly-plugin"
version = "0.1.0"
description = "Shelly device Checkmk extension"
requires-python = "==3.14.*"
dependencies = [
    "cryptography",
    "pydantic",
    "requests",
    "urllib3",
    "vcrpy",
    "httpx",
]

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
